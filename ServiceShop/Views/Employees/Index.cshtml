@model IEnumerable<ServiceShop.Models.Employee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rating)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.ApplicationUserId)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rating)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.ApplicationUserId)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-primary" })
            @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-primary" })
            @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-primary" })
            @Html.ActionLink("All Customer List", "CustomerList", "Customers", new { id = item.Id }, new { @class = "btn btn-primary" })
            @Html.ActionLink("My Customer List", "CustomerList2", "Customers", new { id = item.Id }, new { @class = "btn btn-primary" })
            @*@Html.ActionLink("EmployeeWorkOrder", "EmployeeWorkOrder", "Services", null, new { id = item.Id }) |*@
            @Html.ActionLink("Completed Orders", "CustomerList3", "Customers", new { id = item.Id }, new { @class = "btn btn-primary" })
            @Html.ActionLink("Paid Orders", "CustomerList4", "Customers", new { id = item.Id }, new { @class = "btn btn-primary" })
            @Html.ActionLink("My Work Orders", "Index3", "Services", new { id = item.Id }, new { @class = "btn btn-primary" })
            @Html.ActionLink("All Orders", "Index", "Services", new { id = item.Id }, new { @class = "btn btn-primary" })
        </td>
    </tr>
}

</table>
